// imported lib module here
const lib = require('./lib.js');

// print lib object details
console.log(lib) 

// using object to refer to function and passing values to it
console.log(lib.sum(4,5),lib.diff(9,4));

